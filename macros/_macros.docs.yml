version: 2

macros:
  - name: cents_to_dollars
    description: Converts a cents-denominated numeric column into dollars and rounds to the specified decimal places.
    arguments:
      - name: column_name
        type: string
        description: The name of the cents column to convert
      - name: decimals
        type: integer
        description: Number of decimal places to round to, defaults to 2